# envs/audio/madmom-chord/.env.example
#
# Madmom chord recognition. CPU only — GPU 변수 없음.
# 게이트웨이 통해서만 접근하므로 컴포넌트 자체에는 PORT/AUTH 변수 없음.
#
# Copy: cp .env.example .env.<target>

MADMOM_CHORD_LOG_LEVEL=INFO
